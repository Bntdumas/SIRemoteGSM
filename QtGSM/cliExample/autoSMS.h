#ifndef AUTOSMS_H
#define AUTOSMS_H

#include <QObject>

class QTimer;
class gsmModule;

class autoSMS : public QObject
{
    Q_OBJECT
public:
    explicit autoSMS(QObject *parent = 0);
    
signals:
    
public Q_SLOTS:
    void sendAutoGeneratedSMS();
private:
    QTimer * m_timer;
    gsmModule *m_gsm;

};

#endif // AUTOSMS_H
